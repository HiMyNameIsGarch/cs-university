#include <stdio.h>

/*
 * The client gives the server a file name and an octal number. The server will
 * check if the file has access rights different from the given number. If the
 * rights match, it sends the message ‘All OK!’ if it does not, it will set the
 * rights according to the indicated number and send the message ‘Rights have
 * been changed’. NOTE: The client and server are two processes
 */

int main() {
    return 1;
}

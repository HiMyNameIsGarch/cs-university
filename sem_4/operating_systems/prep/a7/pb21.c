#include <stdio.h>

/*
 * The client sends a username to the server, and the server returns the
 * information indicated by the ‘id’ command for the respective user, or an
 * error message if the name does not indicate a user recognised by the system.
 * NOTE: The client and server are two processes.
 */

int main() {
    return 1;
}

#include <stdio.h>

/*
 * Parent process sends to the child a directory name, and the child sends back
 * to the parent the list of all text files from that directory, or an error
 * message if the directory does not exist.
 */

int main() {
    return 1;
}

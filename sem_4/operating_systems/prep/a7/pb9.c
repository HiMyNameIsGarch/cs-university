#include <stdio.h>

/*
 * Parent process sends to the child a name of a Unix server, and the child
 * sends back to the parent the list of all usernames that work in that moment
 * on the server.
 */

int main() {
    return 1;
}

#include <stdio.h>

/*
 * Parent process sends to the child a directory name, and the child sends back
 * to the parent the total number of bytes from all files from that directory.
 */

int main() {
    return 1;
}

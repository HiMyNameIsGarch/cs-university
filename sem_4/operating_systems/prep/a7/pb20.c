#include <stdio.h>

/*
 * The client sends a file name to the server, and the server returns to the
 * client the names of all directories that contain the specified file. NOTE:
 * The client and server are two processes.
 */

int main() {
    return 1;
}

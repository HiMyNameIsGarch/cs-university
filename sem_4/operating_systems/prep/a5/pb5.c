#include <stdio.h>

/*
 * Write a C Program that receives as command line arguments two words and the
 * name of a file. The program will print at stdout the content of the file,
 * replacing the occurrences of the first word with the second.
 */

int main() {
    return 1;
}


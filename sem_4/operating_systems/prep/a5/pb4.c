#include <stdio.h>

/*
 * Write a C Program that compares the content of two files provided as command
 * line parameters. The comparison will be performed on blocks of 1KB each. The
 * program will print the total number of blocks from each file and the number
 * of identical blocks found.
 */

int main() {
    return 1;
}

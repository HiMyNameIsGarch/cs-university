#include <stdio.h>

/*
 * Write a C Program that receives as command line arguments a name of a file
 * and several words. The program will print at stdout the content of the file,
 * deleting the occurrences of all words provided as arguments.
 */

int main() {
    return 1;
}

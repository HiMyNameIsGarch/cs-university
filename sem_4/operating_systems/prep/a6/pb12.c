#include <stdio.h>

/*
 * Compute the sum of an array of numbers using divide et impera method: a
 * process splits the array in two sub-arrays and gives them to two other chid
 * processes to compute their sums, then adds the results obtained. The child
 * processes use the same technique (split, etc. …).
 */

int main() {
    return 1;
}


